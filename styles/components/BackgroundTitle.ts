import styled from "styled-components";

export const Container = styled.div`
  position: absolute;
  bottom: 0;
  left: 0;
  z-index: -1;

  margin-left: 1rem;

  font-size: 12rem;
  font-weight: bold;
  color: lightgray;

  opacity: 0.35;
  user-select: none;
`;
