import styled from "styled-components";

export const Container = styled.div`
  position: absolute;
  bottom: 1rem;
  left: 0;
  z-index: -1;

  max-width: 100%;
  overflow-x: hidden;

  margin-left: 1rem;

  font-size: 12rem;
  font-weight: bold;
  color: lightgray;

  opacity: 0.25;
  user-select: none;
`;
